{% if user_role == "ROLE_ADMIN" %}
        <li class="nav-item">
            <a class="nav-link" href="{{ path('admin.article.index') }}">Control Panel</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="{{ path('logout') }}">Logout</a>
        </li>
    {% else %}
        <li class="nav-item">
            <a class="nav-link" href="{{ path('login') }}">Connect</a>
        </li>
    {% endif %}
