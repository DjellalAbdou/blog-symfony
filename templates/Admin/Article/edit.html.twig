{% extends 'base.html.twig' %}

{% block navbar %}
    {{ include('_navbar.html.twig', {user_role: user_role}) }}
{% endblock %}

{% block body %}
    <div class="container mt-4 mb-5">
        {{ form_start(form) }}
        <div class="row d-flex justify-content-center">
            <div class="col-md-8">
                {{ form_row(form.title) }}
            </div>
        </div>
        <div class="row d-flex justify-content-center">
            <div class="col-md-8">
                {{ form_row(form.content, { 'attr': {'class': 'textAreaArticle'} }) }}
            </div>
        </div>

        <div class="row d-flex justify-content-center">
            <div class="col-md-8 ">
                <button class="btn btn-primary">Editer</button>
            </div>
        </div>
        {{ form_end(form) }}

    </div>
{% endblock %}

{% block title %}

{% endblock %}

